#' TCGA BRCA Data
#'
#' ExpressionSet Object containing 37335 features and 1222 samples. Counts are filtered to remove
#' low-expressed genes, DESeq2normalized to normalize for library size, and log transformed (log2(x+1)).
"tcga_data"

#' METABRIC BRCA Data
#'
#' ExpressionSet Object containing 24368 features and 1980 samples. Counts are log transformed
"metabric_data"
